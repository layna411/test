version: 2.1

jobs:
  build:
    docker:
      - image: cimg/android:2024.01

    steps:
      - checkout

      - run:
          name: Build
          command: echo "CircleCI is working"

workflows:
  build-and-test:
    jobs:
      - build